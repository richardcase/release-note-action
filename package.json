{
  "name": "release-note-action",
  "version": "1.0.0",
  "description": "Release-notes section like the Kubernetes Prow plugin",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js --license LICENCE",
    "format": "prettier --write src/*.js",
    "format-check": "prettier --check src/*.js",
    "test": "mocha src/**/*.test.js"
  },
  "keywords": [
    "github-action",
    "release-notes"
  ],
  "author": "Richard Case",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.4.0",
    "@actions/github": "^5.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.28.6",
    "chai": "^4.3.4",
    "jest": "^27.0.5",
    "mocha": "^9.0.1",
    "prettier": "^2.3.2",
    "request": "^2.88.2",
    "sinon": "^11.1.1"
  }
}
